services:
  linkedin-ads-library-mcp:
    build: .
    ports:
      - "3001:3000"
    environment:
      - LINKEDIN_ACCESS_TOKEN=${LINKEDIN_ACCESS_TOKEN}
      - MCP_TRANSPORT=http
      - MCP_PORT=3000
    restart: unless-stopped
